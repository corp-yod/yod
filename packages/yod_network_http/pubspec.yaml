name: yod_network_http
description: "A new Flutter package project."
version: 0.0.1
homepage:

publish_to: "none"

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.2
  plugin_platform_interface: ^2.1.8

  yod_data:
    path: ../yod_data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      # web:
      #   pluginClass: CoreNetworkHttpWeb
      #   fileName: src/platform/core_network_http_web.dart
      android:
        dartPluginClass: YodNetworkHttpDart
      ios:
        dartPluginClass: YodNetworkHttpDart
